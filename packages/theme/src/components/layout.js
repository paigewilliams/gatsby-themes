import React from "react";

export default ({ children }) => (
  <div
    style={{
      margin: "0 auto",
      padding: "40px",
      backgroundColor: "#ff6347",
      height: "100vh",
      width: "100vw"
    }}
  >
    {children}
  </div>
);
