import React from "react";

export default ({ children }) => (
  <div
    style={{
      margin: "10%",
      backgroundColor: "#ff6347",
      height: "100vh",
      width: "100vw"
    }}
  >
    {children}
  </div>
);
